/**
 * 话术模板库（试点页）。
 *
 * 它**不属于任何班级**（classScoped=false）—— 故意选它做试点，
 * 就是为了验证注册表与通用列表同时支持「班级范围」和「全局」两种表。
 */

export const templatesPageDef = { specKey: 'templates', group: '家校沟通', iconName: 'message' };
