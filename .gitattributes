# 统一换行与二进制标记：仓库内一律 LF，避免 Windows 上反复出现 CRLF 转换告警
* text=auto eol=lf

# 二进制 / 原件，不做换行转换
*.html   -text
*.png    -text
*.jpg    -text
*.jpeg   -text
*.mp4    -text
*.wav    -text
*.m4a    -text
*.xlsx   -text
*.sqlite -text
*.zip    -text

# 文本类型
*.md   text
*.py   text
*.js   text
*.css  text
*.toml text
*.json text
*.yml  text

# 启动脚本的行尾是**硬要求**（见 CONTRIBUTING §10），不能跟着全局的 eol=lf 走：
#   .bat 必须 CRLF —— 只有 LF 的 .bat 会被 cmd 拼成乱命令（实测「不是内部或外部命令」）；
#   .command / .sh 必须 LF —— 否则 macOS 的 bash 可能拒绝执行。
*.bat     text eol=crlf
*.command text eol=lf
*.sh      text eol=lf
