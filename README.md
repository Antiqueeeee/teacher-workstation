# 班主任工作台（重构版）

把一个买来的「单文件网页应用」（所有数据存在浏览器 localStorage 里）重构为**前后端分离、数据持久化、可在离线内网使用**的项目。

- 原应用：`raw-material/web-app/班主任工作台.html`（18995 行单文件，只读参考，不再修改）
- 设计文档：`docs/改造方案/`（**动手前先读**）
- 开发约定：`CONTRIBUTING.md`（分层禁令、单文件 600 行上限、离线硬规则）

---

## 要解决的问题

1. **数据不再存在浏览器缓存里** —— 从 localStorage 单键搬到 SQLite + 媒体目录。
2. **复刻现有 33 个页面的功能**，逻辑从「前端直读直改全局数据」改为「前端调后端 API」。
3. **三处确定的调整**：
   - 宿舍分布配置时会出现意义不明的报错（根因是数据模型错了，见 `04` §1）；
   - 作业提交率目前靠用户手填，改为后端自动计算（`04` §2）；
   - 新增照片与音频**归档**（录音由老师用手机自行完成，本项目只负责存档，`04` §3）。

## 已确定的关键约束

| 约束 | 说明 |
|---|---|
| 部署形态 | 服务跑在**老师自己的电脑**上，手机/电脑通过内网地址访问 |
| 账号与权限 | **不做**（无账号、无口令、无角色） |
| 离线 | **部署环境不联网**（客户要求）→ 运行期零外网依赖，依赖需预先离线打包 |
| 备份 | 本轮不做；数据就在本地目录，可整包拷走 |
| 多班 | 数据模型支持多个班 |
| 手机端 | 全部 33 个页面都要好用 |
| 不做的 | 账号/口令/权限、备份功能、导出为独立 HTML、桌面外壳 |

完整决策与**已作废、不要实现**的清单见 `docs/改造方案/README.md` §6.1 与 `05-部署与访问设计.md` §7。

## 文档索引

| 文档 | 内容 |
|---|---|
| `docs/改造方案/README.md` | 目标、技术路线、三个痛点的结论、会议对照、已定决定 |
| `01-现状逆向分析.md` | 原应用的架构、存储机制、数据模型、33 页清单、40 条已知缺陷 |
| `02-需求梳理-逐功能.md` | 33 个页面逐项：现状 → 问题 → 改造后需求 |
| `03-目标架构与代码组织.md` | 技术选型、目录结构、API 契约、数据库设计、行数约束、导入与迁移 |
| `04-重点改造与实施路线.md` | 三项重点改造细则、缺陷修复清单、分阶段计划、验收标准、风险 |
| `05-部署与访问设计.md` | 运行环境与已接受的取舍、变更记录（**含不要实现的清单**） |

## 当前状态

- ✅ 设计与决策完成（6 份文档）
- ✅ 仓库初始化、开发约定、目录骨架
- ✅ 开发环境：后端依赖装齐（Python 3.11）
- ✅ 后端地基：配置 / 模型 / Alembic 迁移 / 通用 CRUD 工厂 / 统一错误封套；3 张试点表（待办、班规、话术模板）已跑通，**10 个测试通过**
- ⬜ 阶段 1 余下：导入基础设施 + 演示夹具装载 → 前端 core 层 → 3 个试点页
- ⬜ 后续：学生档案等核心表、媒体库、移动端适配、一键启动包（已推迟）

## 快速开始

```bash
# 1. 装依赖（用仓库里的 .validation/conda，Python 3.11）
../.validation/conda/python.exe -m pip install -r backend/requirements-dev.txt

# 2. 起服务（手机与电脑都通过内网地址访问）
cd backend
PYTHONPATH=. ../.validation/conda/python.exe -m uvicorn app.main:app --host 0.0.0.0 --port 8723 --reload
# → http://<本机内网IP>:8723/

# 3. 跑测试
PYTHONPATH=. ../.validation/conda/python.exe -m pytest -q

# 4. 提交前检查行数
python tools/check_file_size.py
```

首次启动会自动建目录、跑迁移，并创建一个**空的**默认班级（不是演示数据）。
