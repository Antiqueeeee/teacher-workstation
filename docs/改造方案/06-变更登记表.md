# 变更登记表

> 「界面尽量不变」是实施原则之一（`04` 第三部分·原则 1）。本表登记**所有偏离旧应用的
> 可见变化**，随阶段交付一起给用户确认。分两类：
> **A = 修错**（旧应用的行为本身是错的，改掉不需要商量，但要让用户知道）；
> **B = 变更**（旧应用没错，是我们换了做法，**需要用户确认**）。

---

## 阶段 2 · 主数据与高频页

### 作业情况 `homework`

| # | 类 | 变化 | 为什么 |
|---|---|---|---|
| 1 | B | 新增「未交名单」输入（顿号分隔，全交填「无」），**提交率由它自动算出**，不再手填 | 用户明确提的痛点。旧应用的提交率是手填数字（默认 100），与自由文本的名单互不相通 |
| 2 | B | 新增「提交率模式」（自动/手工）与「手工提交率（%）」；只有手工模式才用手填值 | 保留「学校要求按人数上报」这类特殊场景的出口 |
| 3 | A | 「应交人数」留空时按**当时全班人数**存下来（快照），之后学生转入转出不改写历史作业 | 否则「上周的提交率」会自己变化，历史数据失去可比性 |
| 4 | A | 应交 0 人时提交率显示「—」而不是 100% | 旧的 100% 看起来像「全都交了」 |
| 5 | A | 列表新增「未交名单」列；导出多出「提交率（%）」「未交名单」等列，且**导出即导入模板**（导出去改完能直接导回来） | 旧应用导出的列比导入认的列少，互相对不上 |

### 出勤记录 `attendance`

| # | 类 | 变化 | 为什么 |
|---|---|---|---|
| 6 | A | 出勤率口径改为：缺席 = 当天**去重后的缺席学生数**；迟到、早退**单列**不计入出勤率 | 旧应用三处出勤率各算一遍、都按记录条数，一人一天两条会被扣两次 |
| 7 | A | **没登记考勤的那天出勤率显示「—」**，不再显示 100% | 旧应用把「没登记」和「全员出勤」画成同一个 100%，且文案写「今日全员出勤」，是误导 |
| 8 | A | 点名弹窗的选项从「正常/迟到/病假/事假/旷课」变为**含早退的 5 类 + 正常** | 旧应用标不了早退，而提交时的清理名单也漏了早退，早退记录永远删不掉 |
| 9 | A | 点名**按天整体覆盖**：改回「正常」的学生，当天记录会被清掉 | 旧应用只清 4 类，会残留 |
| 10 | A | 点名**不改动没变的那条记录**（老师手填的事由与处理说明留着） | 旧应用「删掉重建」会覆写事由、清空处理情况，于是刚跟进完的学生又变回待办 |
| 11 | B | 「处理情况」自由文本拆成两个字段：**「跟进状态」（待联系/已通知/无需联系）** + 「处理情况」（留档） | 旧应用用一个自由文本既当状态又当备注，无法可靠地驱动待办 |
| 12 | B | 跟进状态默认值按类型判定：**旷课 = 待联系**，病假/事假/迟到/早退 = 无需联系 | 见下「待确认」 |
| 13 | B | 新增**出勤看板**（页面顶部）：某天的出勤率/应到/缺席/迟到早退 + 前/后一天切换 | 旧应用只有列表与统计，没有「今天怎么样」的一眼视图 |
| 14 | A | 一个学生一天**只能有一条**出勤记录（旧的允许一天多条） | 一天多条是出勤率被重复扣减的根源 |
| 15 | A | 出勤记录**不能恢复**（删除即删除），因此列表里不提供「显示已删除」 | 唯一约束（一天一条）与软删除天生冲突：删掉的记录仍占着键，同一天同一个学生就再也登记不进来 |

### 成绩分析 `scores`

| # | 类 | 变化 | 为什么 |
|---|---|---|---|
| 16 | A | 名次**同分并列**（1, 2, 2, 4），并标出「并列」 | 旧应用按数组顺序给名次，同分的学生拿不同名次 |
| 17 | B | 新增**「缺考」标记**（旧应用没有这个概念，留空一律按 0 分） | 缺考的学生在旧应用里必然排最后一名，且与「考 0 分」无法区分 |
| 18 | A | 及格线按**本场实际考试的科目**满分算；且按**该生实际参加科目**的满分算 | 旧应用用「全部科目的并集满分」，某场只考 3 科时全班及格率凭空变低 |
| 19 | B | 新增**「得分率」**列（总分 ÷ 实际参加科目满分） | 150 分制的数学与 100 分制的地理直接相加没有意义，跨考试比较只能看得分率 |
| 20 | A | 单科评价与及格率全部按**得分率**分档（85/70/60），不再用绝对分 | 旧应用在学科模块里硬编码「60 分及格」，与 150 分制不匹配 |
| 21 | A | 改了科目满分**立刻生效**，不需要任何「重算」动作 | 旧应用把名次与总分存在成绩行里，改满分不重算，值一直是错的 |
| 22 | **B** | **「各科满分设置」从全局配置改为每场考试一份**（新建考试按默认值 语数英 150 / 其余 100 生成，再按实际删减） | 见下「待确认」 |
| 23 | B | 成绩录入改为**网格表**（学生 × 科目），一格填数字 / 填「缺」/ 留空（还没录） | 旧应用是逐条录入 + 导入，45 人 × 9 科要开 405 次表单 |
| 24 | A | 「上一场考试」按日期 + id 判定，同一天两场也有确定顺序 | 旧应用按数组顺序取，把「最近一次考试」取成了更早那场，评语的进步/退步方向整个相反 |
| 25 | B | 新增「考试管理」为同一页面的列表区（考试的新增/编辑/删除在原处） | 旧应用的考试管理是同一个页面的一个 Tab，这里改成列表区，交互位置有变化 |

### 全站通用

| # | 类 | 变化 | 为什么 |
|---|---|---|---|
| 26 | B | 侧栏分组与页面顺序按本项目的阶段规划排（不是旧应用的 33 页顺序，因为页面还没全部落地） | 每落地一批页面就补进来 |
| 27 | A | 删除默认是**软删除**（可勾「显示已删除」恢复）；已确认不能恢复的表（出勤）明确说「彻底删掉」 | 旧应用删除即消失；承诺能恢复就必须真能恢复 |
| 28 | A | 导入有**预览 + 冲突报告**，脏行被拒时整批回滚 | 旧应用的「清空并替换」模式绕过全部校验 |
| 29 | A | 错误提示是**具体的中文说明**（例如「学生档案里没有叫『张三』的学生」），不再出现无上下文的报错 | 用户提的痛点之一 |

---

## 待确认（B 类里需要用户拍板的）

1. **出勤「跟进状态」的默认值按类型判定**（#12）：旷课默认「待联系」、请两类与迟到早退默认「无需联系」。
   旧应用的待办中心只对「病假/事假/旷课」且处理情况为空的学生生成待办，
   所以「迟到早退不进待办」与旧行为一致；差别在**旷课现在会进待办**（旧应用点名后
   处理情况被清空，也会进待办 —— 但那是因为数据被覆写，不是有意的设计）。
   如果你希望迟到也要跟进，改的是一个常量。

2. **「各科满分设置」改为每场考试一份**（#22）：这是修「及格线用科目并集」的必要代价 ——
   满分必须属于某一场考试，「本场满分」才能是个明确数字。代价是改满分要去那一场考试里改。
   如果你更习惯旧应用那种全局开关，可以加一个「一次改、并同步到所有考试」的批量操作。

---

## 阶段 3 · 宿舍与座位

### 宿舍分布 `dorms`

| # | 类 | 变化 | 为什么 |
|---|---|---|---|
| 30 | A | 数据模型从「一行 = 一个床位」改为**房间 + 床位两张表**；容量成为房间的正式字段 | 旧应用把容量寄存在「该房间数组顺序第一条记录」上，种子数据里根本没这个字段，于是永远回退 8 人间（`:13685`）；设过的容量重开就没了 |
| 31 | A | 改容量从 `window.prompt()` 改为**应用内模态框** | prompt 在部分环境里被禁用/抛异常，旧应用这时**静默什么都不做**（`:13824`） |
| 32 | A | 容量不能改到小于已住人数，被拒时**列出需要先搬走谁** | 旧应用改了之后界面上直接少显示几个人，库里什么都没变 |
| 33 | A | 床位号是**整数**，「1号床」「01」「1」都认成同一张床；认不出来的写法（如「靠窗」）报错 | 旧应用把「靠窗」解析成 0（`:13645`），那条记录从此在网格里看不见、满员判定也跟着错 |
| 34 | A | 加人入口从**三个**收成一个：看板上点空格子 | 旧应用的工具栏新增 / 点空格子 / 房间底部「增添住宿生」各自处理容量与床号，同一个房间在不同入口下行为不同 |
| 35 | B | 新增**宿舍分布看板**（一间房一张卡、N 个床位格子、空位与满员一眼可见、未分配住宿生数） | 旧应用有卡片与列表两个视图，但列表视图看不到「谁还没床」 |
| 36 | A | 学生从档案删掉后，他的床位在卡片上**标红并提示腾空** | 软删除的学生不会连带删床位，床位号仍被占着 —— 旧应用会让一个查不到的人继续占着床位 |
| 37 | A | 一个学生只能有一张床；同一床位的重复分配会告诉你是谁住的 | 旧应用没有约束，重复床号会让「已住人数」少算、满员判不出来 |

### 宿舍值日 `dormDuty`

| # | 类 | 变化 | 为什么 |
|---|---|---|---|
| 38 | A | 房间改为**引用已有房间**（不再是一段可随便填的房号） | 旧应用的楼栋/房号下拉只复制了一份选项、不校验存在（`:13947`），能造出指向不存在宿舍的值日安排 |
| 39 | A | 值日学生改为**引用学生**（不再只存姓名） | 旧应用只存姓名，学生一改名这条记录就与人对不上 |
| 40 | A | 星期存**序号**（显示仍是「星期一」） | 旧应用按 `indexOf` 排序，词表里出现没见过的写法就排到最前面 |
| 41 | B | 新增**按房间分组的看板**（一间房一张卡，周一到周日各自的安排，点「＋」按天加人） | 旧应用也有分组卡片，但它是前端把整表拉下来分组 —— 记录一多到分页就会少一块而看不出来；这里的 `days` 由后端给全 |
| 42 | A | 房间被删掉后，它的值日安排在看板上**单独列出来**（`orphanRooms`） | 不让记录静默消失 |

### 座位安排 `seats`

| # | 类 | 变化 | 为什么 |
|---|---|---|---|
| 43 | A | 随机排位**不再清空备注**（备注跟人走） | 旧应用填空格时强制 `note:''`（`:10700`），视力/身高的备注全丢 |
| 44 | B | 新增**「锁定」标记**：锁定的座位不参与随机排位与轮换 | 旧应用没有这个概念，想保留的安排挡不住 |
| 45 | B | 批量操作（随机/轮换/交换/清空）后可以**回退一次** | 旧应用直接整体替换 `DB.data.seats`（`:10704`），做错了没有退路 |
| 46 | A | 行列加唯一约束，重复格在写入时就被拒绝 | 旧应用手工新增/编辑不校验冲突（`:10792`），后写覆盖 → 造出「网格里看不见、列表里还在」的格子 |
| 47 | A | 缩小座位表时先看现有座位，**会把座位挪出格子外的操作直接拒绝**并列出是哪些 | 旧应用改行列数不校验，座位会落到格子外面（看不见但还在） |
| 48 | A | 座位不够时自动加排，并**如实报出来** | 旧应用会悄悄把 `seatPlan.rows` 撑大 |
| 49 | B | 交互改为**点选交换**（点一个座位选中，再点另一个交换/挪动），不依赖拖拽 | 手机上没有拖拽。旧应用桌面端只能拖，手机端另有一套点选逻辑，两套实现 |
| 50 | A | 备注在格子上**看得见**（小标记 + 悬浮显示全文） | 旧应用只放在 `title` 属性里，连打印都印不出来 |
| 51 | A | 「组」由列决定（派生值），不再单独存一份 | 旧应用存了一份又重算（`:10730`），两处会漂 |

### 全站通用

| # | 类 | 变化 | 为什么 |
|---|---|---|---|
| 52 | A | 表格头在**派生列**上不再可点（点了会 500） | 旧应用多处表头点了报无上下文的错；这里的规则是「能排序的必须是真实列」 |
| 53 | A | 表单的「编辑 or 新增」按**有没有 id** 判定 | 看板上点某天新增时，房间与星期需要预填；旧写法只要传了对象就当编辑，没法做预填新增 |

**一处实现方式的变化（不影响界面）**：`schemas/registry.py` 按 `CONTRIBUTING.md` §3
的「按聚合分」拆成 `table_spec.py`（三个类型）+ `specs/{classroom,contact,teaching,dorm}.py`
（各域声明），registry 只留汇总与访问器。既有的 import 路径全部继续可用。

---

## 阶段 4 · 媒体库与沟通留档

### 媒体库（新能力）

| # | 类 | 变化 | 为什么 |
|---|---|---|---|
| 54 | A | 照片与录音**落盘保存**（`data/media/年/月/`），库里只存元数据 | 旧应用把图片以 base64 data URL 塞进那一个 localStorage 键：配额是硬伤、导出会涨到几百 MB |
| 55 | **B** | 新增**录音归档**能力：老师用手机录完之后**上传**（不拦格式），页内播放 | 旧应用**整个应用没有录音能力**。这是用户明确要的第三项调整 —— 注意本项目**不在页内录音、不要麦克风权限**，录音是老师在校外用手机完成的 |
| 56 | A | 图片**自动按 EXIF 方向摆正**，另生成 320 / 1200 两档缩略图 | 旧应用不处理方向，手机拍的照片在列表里常常躺着 |
| 57 | A | 尺寸、时长由**后端读**（不靠用户填） | 老师不该被要求填这些 |
| 58 | A | 同一份文件重复上传**不重复占盘**（按内容指纹去重） | 手机里同一张照片存两遍很常见 |
| 59 | A | 删除进**回收站**（`data/trash/日期/`），可恢复；另有「按日期清理」入口（默认只清录音） | 旧应用删了就没了；录音留档涉及隐私，需要一个明确的清理出口 |
| 60 | A | 存储占用改为**真实遍历目录** | 旧应用按 5 MB 估算（`:16711`），早就不准 |
| 61 | B | 浏览器放不了的录音格式（`amr`/`aac` 等手机上常见的）**标注「需下载后播放」**，不做服务端转码 | 转码要引入 ffmpeg 依赖，与「离线、自带依赖」的原则冲突。标出来比假装能播好 |
| 62 | A | 上传有大小与格式限制，超限给一句说清的话 | 旧应用没有限制，一个几 MB 的 base64 就能把那一键写崩 |

### 家长联系日志（沟通留档）

| # | 类 | 变化 | 为什么 |
|---|---|---|---|
| 63 | A | 学生改为**引用**（不再只存姓名） | 旧应用按姓名做 KPI 去重，重名就误算 |
| 64 | A | 每条记录可以挂**照片与录音附件**（列表每行一个「附件」入口） | 旧应用这个模块**没有附件能力**，而「和家长沟通之后留档」正是用户提的需求 |
| 65 | **B** | 新增两个字段：**联系方向**（去电/来电/面谈/线上）与**结果**（已沟通/未接通/待再联系） | 旧应用的「方式」说的是渠道，说不清「谁打给谁、接没接通」；而老师回看时最需要的是「上周给张伟妈妈打了两次都没接」。**这两个字段是我加的，词表可以改** |
| 66 | A | 「需要再次联系」的记录由后端统一的跟进接口给出 | 旧应用首页与列表页各筛一遍，两处口径可能不一致 |

### 全站通用

| # | 类 | 变化 | 为什么 |
|---|---|---|---|
| 67 | A | 支持附件的模块在列表里显示**附件数** | 否则老师得逐条点进去才知道哪次沟通留了录音 |
| 68 | A | 「按姓名找学生」的规则（查无此人/重名都不猜）收成**一份实现** | 原先监护人、出勤、宿舍各写一遍，沟通留档要写第四遍 —— 同一规则多处实现是这个项目旧版垮掉的方式 |

**阶段 4 还没做的**：其余 6 个沟通类模块（家访/谈话/班会/班级活动/大事记/矛盾调解）的附件 ——
它们本身就是阶段 5 的页面，落地时在表声明里加一行 `media_owner=True` 即可挂附件。

---

## 阶段 5 · 其余页面与收口

### 概览类页面

| # | 类 | 变化 | 为什么 |
|---|---|---|---|
| 69 | B | 新增**工作台首页**：今日待办四张卡（考勤登记/作业待交齐/家长再联系/待办）+ KPI + 跟进清单 + 最近动态 + 常用入口 | 旧应用首页有 15 张表的聚合，但那张「作业待收」读的是两个**根本不存在的字段**（恒为 0）。这里每个数都从所属模块的口径服务取 |
| 70 | B | 跟进清单**按权重排、每条能落到具体记录**（带搜索词跳到该模块列表） | 旧应用只跳到模块首页，老师还得自己找是哪一条。权重与窗口集中在 `FOLLOWUP_RULES` 一处（旧应用硬编码在计算函数里） |
| 71 | B | 新增**数据看板**：出勤趋势、违纪分布与 Top、缺勤 Top、月度走势 | 见下两条 |
| 72 | A | 出勤趋势里**未登记的日子折线断开**，纵轴范围按数据自适应 | 旧应用把没登记画成 100%、纵轴写死 `min:60,max:100`：两者都让「没数据」和「数据好」长得一样 |
| 73 | A | Top 榜按 **学生去重** | 旧应用按姓名，重名会合并成一个人 |
| 74 | B | 新增**学生一生一档**（学生档案列表每行的「档案」入口）：考勤/作业/成绩/纪律/沟通/调解/资助/体质一屏看完 | 旧应用有 12 张表的档案区块，但各段的数是前端现算的，与详情页对不上。这里后端聚合，各段的数取所属模块的口径 |
| 75 | B | 新增**评语草稿**（学生档案列表每行的「评语」入口）：按记录拼初稿 + 各段依据 + 可编辑可复制 + 末尾「请人工复核」 | 见下「待确认」第 3 条 |
| 76 | B | 新增**代课 / 交接简报**（某一天的班级情况，可打印） | 旧应用也有，但它把课表/缺席/体质/纪律/班规/座位图各算一遍 |
| 77 | A | 简报里没登记考勤的那天**明说**「缺席名单可能不准」；缺的段如实写出来 | 不假装全员出勤，也不留空白让人猜 |

### 主数据与沟通留档类页面（15 个模块）

| # | 类 | 变化 | 为什么 |
|---|---|---|---|
| 78 | A | 学生一律改 `student_id` 引用（家访/谈话/班委/团员/违纪/体质/助学/矛盾调解） | 旧应用靠保存时按姓名反查，重名挂错人、改名断链 |
| 79 | A | 联系电话/性别/学号改成**从档案现取**，不再存快照 | 旧应用是回填一次的快照，学生换号就过期了 |
| 80 | A | 矛盾调解的「涉及学生」改**多人子表** | 旧应用存顿号分隔的字符串，一生一档用 `parties.includes(name)` 匹配 —— 姓名互为子串会挂错人 |
| 81 | A | 值日成员改**学生关联子表** | 旧应用是自由文本，统计不出「个人值日次数」 |
| 82 | B | **18 个模块支持照片与录音附件**（列表每行一个「附件」入口） | 用户提的第三项调整；旧应用只有班会/活动有图片字段，且与种子数据对不上 |
| 83 | A | 特殊体质**一人一条**（写入时检查并给中文说明） | 旧应用可给同一个人建多条，应急时看到互相矛盾的两条 |
| 84 | B | 团员名册与档案「政治面貌」的**对不上清单**（只提示、不改写） | 两份数据都可能是事实（刚入团还没更新档案），系统替老师选一个最容易出怪事 |
| 85 | A | 违纪按学生统计；班会出席拆成实到/应到并校验 | 旧应用按姓名统计；出席是一个「45/45」的自由文本框 |
| 86 | B | 值日概览覆盖**全周**（旧应用只渲染周一至周五） | 周六周日的值日在概览里看不见 |

### 班级费用

| # | 类 | 变化 | 为什么 |
|---|---|---|---|
| 87 | A | 金额**以分存整数**（输入仍是元） | 旧应用直接用浮点的元，`0.1 + 0.2` 会出现小数尾数，而这是要跟家长对账的钱 |
| 88 | A | 缴费状态（已缴/部分/未缴/免缴）**在后端推导**，列表/导出/筛选/汇总同一份 | 旧应用只在前端算，导出文件里没有这一列、导入回来也认不出 |
| 89 | A | 收支方向是**显式枚举**；同日流水按录入顺序排 | 旧应用把「不是支出」一律当收入；同日流水只按日期字符串排，顺序随机 |
| 90 | A | 改应缴标准默认**不回填**历史记录（要回填得显式说明，且只回填一分钱没缴过的） | 应缴是收钱那一刻的约定；已缴过的改了会凭空变成「部分缴纳」 |
| 91 | B | 新增**催缴名单**（按欠额排）与**漏收名单**（一条记录都没有的在册学生） | 旧应用没有这两个入口 |

### 设置与收口

| # | 类 | 变化 | 为什么 |
|---|---|---|---|
| 92 | B | 新增**设置页**：班级信息（可配的年级/班号/班主任/教室/团支部名）+ 真实占用 + 每张表行数 | 旧应用的教室名/团支部名是靠「年级 + 班号」字符串拼出来的 |
| 93 | B | **清空数据**要手输「清空」两个字、执行前列出每张表会删多少行、对**所有已知表**生效、照片与录音默认不动 | 旧应用只清种子键（懒建的表被整体丢弃）；这是不可撤销的操作，不该顺手就能点 |
| 94 | B | 新增**课程表**（星期 × 节次，一格一门课）+ **倒计时**（「还有几天」后端算） | 旧应用的课表同一格能存两门课，界面上只显示一门；**OCR 导入不做**（`05` §7 已作废） |
| 95 | B | **时间轴**做成首页的一个区块（不再单开页面） | 旧应用是前端把 6 张表拉下来合并；这里后端合，加一类留档只改一处 |

**阶段 5 还没做的**：**学科与成绩 `courses`**（课程 → 班级 → 学生/作业/成绩 的嵌套结构）。
它是旧应用里给**任课教师**用的第二套成绩体系，与其他模块的重叠最多（文档也要求把课程作业
并进 `homework`），所以放在最后单独做 —— 不做完就不算阶段 5 收尾。

---

## 后续工作

阶段 5 评审之后的工作在评审记录里追加。

### 阶段 2 验收标准的两处调整（`04` 阶段 2 相应条目同步修订）

1. **「学生档案：同名与学号冲突全部进报告且可人工裁决」**
   - 已实现「同名」部分：`GET /api/v1/students/name-conflicts` 一次列全，档案页顶部提示并给出处理建议。
   - 「学号冲突」**不需要报告**：`students` 上有部分唯一索引
     `uq_students_class_sno(class_id, sno) WHERE sno <> ''`，同学号在结构上写不进去，
     写入口还会返回中文说明。靠结构保证比靠事后检测可靠。
2. **「作业：学生档案欠交次数、首页待收、科目平均率三处数值一致」与「一生一档」**
   - 推迟到**阶段 5**实现。三处要读的 `homework_unsubmitted` 关系与统计口径现在都已就位
     （口径唯一，见 `services/homework_service.py`），但「首页」与「一生一档」是阶段 5 的页面范围，
     在它们落地之前无法验收「三处一致」。**这是有意的推迟，不是漏做** —— 阶段 5 收口时会
     用同一份夹具数据逐项核对这三处的数值。
