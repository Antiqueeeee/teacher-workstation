# 班主任工作台 · 使用说明

给老师看的。三步就能用起来。

---

## 一、第一次使用

1. 把整个文件夹解压到**你自己电脑**上（想放哪都行，比如桌面或 D 盘；文件夹里已经自带
   Python 运行时，不需要你另外装 Python，也不需要装 Docker 或数据库）。
2. **双击「启动」**（Windows 是 `启动.bat`，Mac 是 `启动.command`）。
3. 屏幕上会出现一个黑窗口，里面写着两个网址：
   - 这台电脑上打开：`http://127.0.0.1:8723/`
   - 手机/平板打开：`http://192.168.x.x:8723/`
   浏览器会自动打开界面。

> 第一次运行时 Windows 可能弹出「是否允许访问网络」的提示 —— 请勾选**专用网络**并允许。
> 否则手机连不上这台电脑。

**手机怎么用**：让手机连上**和电脑同一个 WiFi**，然后二选一 ——

- **扫二维码**（最省事）：黑窗口里会画出一张二维码，用手机相机或微信扫一下就等于打开了；
- **手输地址**：在手机浏览器里输入黑窗口里那个 `http://192.168.x.x:8723/`（建议存成书签）。

界面上也能看到这张二维码：**设置 → 手机访问**（那里还有个「复制地址」按钮，
可以把地址发到手机上打开）。

> **手机连不上？** 先确认手机和电脑是同一个 WiFi；如果电脑上装了 VPN、虚拟机或
> 多个网卡，程序选的那个地址可能手机到不了 —— 去**设置 → 手机访问**里用
> **「换一个地址」**试另一个（黑窗口里也会把候选地址列出来）。

## 二、平时怎么开、怎么关

| 想做的事 | 双击这个 |
|---|---|
| 用的时候打开（黑窗口开着，关掉窗口就停） | `启动` |
| 让它一直在后台跑（没有窗口，关掉也不影响） | `后台启动` |
| 停掉服务 | `停止` |
| 看它现在跑没跑、手机该访问哪个地址 | `状态` |
| 开机自动在后台跑起来（不用每次点） | `设置开机自启`（不想要了就点 `取消开机自启`） |

建议：平时用**「后台启动」+「设置开机自启」**——以后电脑一开机它就在，你只要在手机或电脑上
打开那个网址就行。

## 三、你的数据在哪（重要）

**所有数据都在这个文件夹里的 `data` 目录里**：数据库、照片、录音、日志。

- **备份**：把这个 `data` 目录整个复制一份（拷到 U 盘、另一个盘、网盘都行），
  需要时把它放回来即可。系统里**没有**自动备份功能，请自己定期拷。
- **换电脑**：把整个文件夹拷过去，再把 `data` 目录也拷过去。
- **升级**：替换程序文件即可，**别删 `data` 目录**（那是你的全部数据）。
- **清空数据**：在界面「设置」页里有「清空数据」（要手输「清空」两个字），照片与录音默认不动。

## 四、这台电脑上的 Python 会不会被动？

**不会。** 这个项目**自带一份 Python**（放在项目里的 `runtime` 文件夹），只给它自己用：

- **第一次运行时**，如果 `runtime` 还不存在，启动脚本会自动准备它：下载一份可搬移的 Python，
  把依赖装进 `runtime` 里面（**需要联网一次**；之后就完全离线可用）；
- 它**不会**安装、修改或卸载你电脑上已有的 Python，也**不会**改 PATH 之类的系统环境变量；
- 你电脑上的 Python 最多被用来「跑一下准备脚本」（下载与安装），准备完就再也不用了 ——
  跑服务的**永远**是这个项目自带的那份；
- 所有数据都写在**这个文件夹**的 `data` 里，不往别处写。

也就是说：**卸载 = 直接把这个文件夹删掉**（如果设过开机自启，先点一次「取消开机自启」）。
万一 `runtime` 文件夹被删了或复制不全，启动脚本会明确告诉你怎么补，而**不会**去
拿你电脑上的 Python 硬跑。

## 五、用别的工具启动（快捷启动器 / 定时任务 / AI 助手）

有些老师会用一个「启动器」程序、定时任务工具，或者 WorkBuddy 这类 AI 助手来把服务拉起来。
这个服务对它们来说就是**一条命令**，放在这个文件夹里执行：

| 想做的事 | 命令（在包根目录执行） |
|---|---|
| 后台启动（无窗口，推荐给工具调用） | `runtime\python.exe launcher.py --daemon`（macOS：`runtime/bin/python3 launcher.py --daemon`） |
| 前台启动（有窗口，关窗即停） | `runtime\python.exe launcher.py` |
| 看它跑没跑（机器可读） | `runtime\python.exe launcher.py --status --json` |
| 停止 | `runtime\python.exe launcher.py --stop` |

**给工具作者/助手看的约定**（都可直接依赖）：

- **重复启动是安全的**：已经在跑时，`--daemon` 会直接返回 0（不会起第二个实例）；
- **退出码**：0 = 成功（或本来就在跑），1 = 失败/没在跑（`--status` 时）；
- **`--status --json`** 输出一行 JSON：
  `{"ok": true, "running": true, "pid": 123, "port": 8723, "local": "...", "lan": "...", "dataDir": "...", "startedAt": "..."}`；
  没在跑时是 `{"ok": true, "running": false}`；
- **想确认真的起来了**：请求 `http://127.0.0.1:<port>/api/v1/health`（返回 `{"ok":true,...}`）；
- **环境变量**（都可选）：`TWS_PORT` 起始端口、`TWS_DATA_DIR` 数据目录、`TWS_HOST` 监听地址；
- **它自己写了什么**：数据目录里的 `server.json`（进程号与端口）与 `server.lock`（启动锁），
  `data/logs/app.log` 是日志。工具想清理时，只删这两个文件没用 —— 请用 `--stop`。

> 提醒：**手机能访问的前提是那台电脑开着、服务在跑**。用工具启动也一样，
> 电脑关机或睡眠了手机就连不上（这是既定设计，不是故障）。

### 用 WorkBuddy 这类 AI 助手启动（说一句话就行）

WorkBuddy 是腾讯的桌面 AI 助手，它能替你在本机跑命令 —— 那就等于**有人替你双击了「启动」**。
可以这样跟它说（把文件夹路径换成你的实际路径）：

- 「帮我启动 `D:\班主任工作台` 里的服务：运行 `runtime\python.exe launcher.py --daemon`，
  然后告诉我手机该访问哪个地址」；
- 「看看它跑没跑：`runtime\python.exe launcher.py --status --json`」；
- 「帮我停掉：`runtime\python.exe launcher.py --stop`」。

**三条提醒**：

1. **不要让它装 Python 或装依赖** —— 这个包自带 Python 运行时，不需要装任何东西。
   让它去装东西只会污染你自己的 Python 环境（而且它装的东西这个程序也用不上）。
2. **开机自启别指望它** —— AI 助手不是服务管理器。要开机就自动跑，请用「设置开机自启」，
   那是唯一可靠的路径。
3. **让它用 `--status --json` 或手机复核，别只听一句「已经启动了」** ——
   有些助手会把命令丢在它自己的会话里，关掉客户端（或重启）可能连服务一起停掉。
   最实在的复核方式：**拿手机或浏览器打开那个地址，能打开就是真的在跑**。

## 六、常见问题

**手机打不开？**
1. 确认手机和电脑连的是**同一个 WiFi**（同一个路由器）；
2. 确认那台电脑**开着**、服务在跑（用「状态」看一下，或者黑窗口还开着）；
3. 第一次运行时 Windows 的防火墙提示要点「允许」；
4. 电脑上装的安全软件（360、电脑管家）有时会拦「局域网访问」，把它放行。

**地址不是 8723？**
如果 8723 被别的程序占用了，程序会自动换成 8724、8725……**以黑窗口里显示的那个为准**。

**Mac 上双击没反应 / 提示没有权限？**
右键点 `启动.command` → 选「打开」→ 再确认一次「打开」。如果提示来自身份不明的开发者，
在「系统设置 → 隐私与安全性」里允许一次即可。还不行就在终端里执行一次：
`chmod +x 启动.command 后台启动.command 停止.command 状态.command`。

**想换一个端口或者换数据目录？**（一般不需要）
在启动脚本里加环境变量：`TWS_PORT`（起始端口，被占用仍会自动往后找）、`TWS_DATA_DIR`（数据目录）。
注意：只用命令行起服务时，界面与二维码里的地址也以 `TWS_PORT` 为准。

**出问题了怎么办？**
把 `data/logs/` 里的 `app.log` 和 `server.out.log` 发给技术同事 —— 里面有出错原因，
不含学生姓名与电话。
